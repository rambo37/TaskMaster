{
  "name": "taskmaster",
  "version": "1.0.0",
  "description": "A full-stack task manager application built with the MERN (MongoDB, Express, React, Node) stack.",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "client-install": "cd client && npm install",
    "client-start": "cd client && npm start",
    "server-install": "cd server && npm install",
    "server-start": "cd server && nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.0.2"
  }
}
